import { createTheme } from '@material-ui/core/styles';
const theme = createTheme({
  palette: {
    primary: {
      main: '#141414',
    },
    secondary: {
      main: '#696969',  
    },
    default:{
      main:'#141414'
    }
   
  },

});

export default theme;